# web

Next.js 15 app for the Decision Engine Playground: the site, the chat playground, the Model Studio and the API gateway.
See the repository README for architecture, environment variables and deployment. `npm run dev` after copying `.env.example` to `.env.local`.
